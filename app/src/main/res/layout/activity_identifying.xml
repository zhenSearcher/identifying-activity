<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/blur_background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/sharedimage"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".IdentifyingActivity">

    <ImageView
        android:id="@+id/medium_img"
        android:layout_width="@dimen/default_small_img_width"
        android:layout_height="@dimen/default_small_img_height"
        android:adjustViewBounds="true"
        android:scaleType="centerInside"
        android:src="@drawable/pic0010" />

    <LinearLayout
        android:layout_width="@dimen/default_small_img_width"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <TextView
            android:id="@+id/identifying"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/identifying"
            android:textColor="#dddddddd"
            android:textSize="18sp" />
        <ProgressBar
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginLeft="80dp" />
    </LinearLayout>

    <View
        android:layout_width="@dimen/default_small_img_width"
        android:layout_height="1dp"
        android:background="@android:color/white" />

</LinearLayout>
